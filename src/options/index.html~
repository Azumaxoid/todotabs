<!DOCTYPE html>
<html>
<head>
  <title>TodoTabs</title>
  <style>
.title_bar {
  position: relative;
  margin-top: 8px;
  height: 24px;
}
.title {
  font-size: 16px;
  color: #888;
}
.btn {
  padding: 3px;
  margin: 0 8px 0 8px;
  border-radius: 2px;
  cursor: pointer;
}
.btn:hover {
  background-color: #ddd;
}
.name {
  position: relative;
  width: 480px;
  overflow: hidden;
  margin-left: 16px;
  height: 16px;
  display: inline-block;
  line-height: 16px;
  color: #0B0080;
}
.url {
  margin-left: 8px;
  text-decoration: none;
  cursor: pointer;
}
.delete {
  visibility: hidden;
  position: relative;
  top: -3px;
  width: 16px;
  height: 16px;
  color: #aaa;
  margin-left: 8px;
}
.delete:hover {
  color: #000;
  cursor: pointer;
}
.item:hover > .delete, .item:hover > .todo {
  visibility: visible;
}
.item:hover > .time {
  color: #333;
}
.item:hover > .url > .name {
  text-decoration: underline;
}
.time {
  color: #888;
}
.todo {
  visibility: hidden;
  position: relative;
  top: -3px;
  width: 16px;
  height: 16px;
  text-align: center;
  display: inline-block;
  transform: rotateY(180deg) rotateZ(-45deg);
  color: #fff;
  background-color: #ccc;
  font-weight: bold;
  border-radius: 50%;
}
.todo:hover {
  background-color: #888;
  cursor: pointer;
}  
  </style>
</head>
<body>
  <div id="contents">
    <div class="content">
      <div class="title_bar">
        <span class="title"></span>
        <span class="open_all btn">Open All</span><span class="delete_all btn">Delete All</span>
      </div>
      <div class="items">
        <div class="item" draggable="true">
          <span class="drag"></span>
          <span class="delete">X</span>
          <span class="url" target="_blank">
            <img class="favicon" width=16 height=16>
            <span class="name"></span>
          </span>
          <span class="time"></span>
        </div>
      </div>
    </div>
  </div>
  <script src="/js/tab-list.js">
  </script>
</body>
</html>
